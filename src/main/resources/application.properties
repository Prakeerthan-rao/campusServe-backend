# ============================================================
# Server
# ============================================================
server.port=${PORT:8080}
server.address=0.0.0.0

# ============================================================
# DataSource
# ============================================================
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://prakeerthan:ftqbB8ZuYQD7hD7ZhkV6HkLuIPsXU07e@dpg-d71ptfn5gffc738128ug-a/campus_service_db}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:prakeerthan}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:ftqbB8ZuYQD7hD7ZhkV6HkLuIPsXU07e}
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.hibernate.ddl-auto=update
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# ============================================================
# JWT
# ============================================================
jwt.secret=${JWT_SECRET:campusServe-super-secret-jwt-key-2024-render}
jwt.expiration-ms=${JWT_EXPIRATION:86400000}

# ============================================================
# Logging
# ============================================================
logging.level.org.springframework=INFO
logging.level.com.campus=DEBUG
```

**What changed:** Added `spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect` back — when Hibernate can't connect to the DB to auto-detect the dialect, it needs this line as a fallback.

---

**But also check this in Render — the real problem is likely here:**

Go to **Web Service → Environment** and confirm `SPRING_DATASOURCE_URL` is set **exactly** as:
```
jdbc:postgresql://prakeerthan:ftqbB8ZuYQD7hD7ZhkV6HkLuIPsXU07e@dpg-d71ptfn5gffc738128ug-a/campus_service_db
