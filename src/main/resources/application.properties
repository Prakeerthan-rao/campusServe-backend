# ============================================================
# Server — Render injects $PORT dynamically; fallback is 8080
# ============================================================
server.port=${PORT:8080}

spring.datasource.url=jdbc:postgresql://prakeerthan:ftqbB8ZuYQD7hD7ZhkV6HkLuIPsXU07e@dpg-d71ptfn5gffc738128ug-a.oregon-postgres.render.com/campus_service_db

spring.datasource.username=prakeerthan

spring.datasource.password=ftqbB8ZuYQD7hD7ZhkV6HkLuIPsXU07e



# ============================================================
# DataSource — set these in Render's Environment Variables tab
# ============================================================
spring.datasource.url=${SPRING_DATASOURCE_URL}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# ============================================================
# JWT — set JWT_SECRET in Render's Environment Variables tab
# Must be a long random string (at least 32 characters)
# ============================================================
jwt.secret=${JWT_SECRET}
jwt.expiration=${JWT_EXPIRATION}

# ============================================================
# Logging
# ============================================================
logging.level.org.springframework=INFO
logging.level.com.campus=DEBUG
